# 필수 패키지
Flask==3.1.2
Flask-CORS==6.0.1
pandas==2.3.3
numpy==2.3.4
scikit-learn==1.7.2
matplotlib==3.10.7
seaborn==0.13.2

# ============================================================
# 프로젝트 실행 방법
# ============================================================
#
# 1. 가상 환경 생성 (처음 한 번만)
#    python -m venv .venv
#
# 2. 가상 환경 활성화
#    macOS/Linux: source .venv/bin/activate
#    Windows: .venv\Scripts\activate
#
# 3. 패키지 설치
#    pip install -r requirements.txt
#
# 4. 프로젝트 실행
#    python app.py
#
# ============================================================
# 중요 주의사항
# ============================================================
#
# ⚠️ improved_emotion_model.py 사용 시 주의:
#
# - 첫 실행 시 모델 학습에 시간이 소요됩니다 (약 1-2분)
#   * 텍스트 감정 분석 모델: 165,017개 샘플로 학습
#   * 색상 모델: 29,446개 샘플로 학습
#
# - 필요한 데이터 파일:
#   * our_model/emotion_sentimen_dataset.csv (텍스트 감정 분석용)
#   * our_model/your_file_name.csv (색상 추천용)
#
# - 모델이 없거나 로드 실패 시:
#   * 자동으로 simple_emotion_model.py로 폴백됩니다
#   * 기본 색상 매핑만 사용됩니다
#
# - 성능 최적화:
#   * 모델은 서버 시작 시 한 번만 로드됩니다
#   * 이후 요청은 빠르게 처리됩니다 (< 100ms)
#
# ============================================================
# 문제 해결
# ============================================================
#
# Q: 모델 로딩이 실패했습니다
# A: 다음을 확인하세요:
#    1. our_model/ 폴더의 CSV 파일이 존재하는지 확인
#    2. 필요한 패키지가 모두 설치되었는지 확인
#    3. 콘솔 로그에서 에러 메시지 확인
#
# Q: 서버가 느립니다
# A: 첫 실행 시 모델 학습으로 인해 느릴 수 있습니다
#    이후 요청은 정상 속도로 처리됩니다
#
# Q: 포트 8080이 이미 사용 중입니다
# A: app.py의 마지막 줄에서 포트 번호를 변경하세요
#    app.run(host='0.0.0.0', port=8081, debug=False)
#
# ============================================================